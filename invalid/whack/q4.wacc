# how much wacc could a whack wacc whack,
# if a whack wacc could whack wacc

begin
    # read null;
    int[] arr = [1, 2, 3];
    if null == null then begin
        println len arr
    end else skip fi
end
